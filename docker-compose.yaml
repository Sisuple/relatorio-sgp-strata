services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: relatorio-sgp-strata:latest
    container_name: relatorio-sgp-strata
    ports:
      - "127.0.0.1:8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
